# name: Playwright Docker Tests
# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]

# jobs:
#   docker-playwright-tests:
#     runs-on: ubuntu-latest
    
#     steps:
#     - uses: actions/checkout@v4
    
#     # Build Docker image
#     - name: Build Docker image
#       run: docker build -t playwright-tests .
    
#     # Run tests inside Docker with explicit report generation
#     - name: Run Playwright tests in Docker
#       run: |
#         mkdir -p test-results
#         docker run --rm \
#           -v $(pwd)/test-results:/app/test-results \
#           playwright-tests \
#           npx playwright test --reporter=junit,html
    
#     # Debug: List contents of test-results directory
#     - name: Check test results directory
#       run: |
#         ls -la test-results
#         pwd
    
#     # Upload test results
#     - name: Upload test results
#       if: always()
#       uses: actions/upload-artifact@v4
#       with:
#         name: playwright-test-results
#         path: |
#           test-results/
#           playwright-report/
#         retention-days: 30

#     # Check test results
#     - name: Check test report
#       if: always()
#       run: |
#         echo "Checking test results directory contents:"
#         find . -name "report.xml"
        
#         if compgen -G "test-results/*.xml" > /dev/null; then
#           echo "Test report XML found"
#           cat test-results/*.xml
#         else
#           echo "No test report XML found"
#           exit 1
#         fi